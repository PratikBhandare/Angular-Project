import { CardContentPipe } from './card-content.pipe';

describe('CardContentPipe', () => {
  it('create an instance', () => {
    const pipe = new CardContentPipe();
    expect(pipe).toBeTruthy();
  });
});
